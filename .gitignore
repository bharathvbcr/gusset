target/
*.a
*.test
bench/results/*.tmp

# macOS
.DS_Store

# DevCouncil (ignore state, caches, databases; track config.yaml)
.devcouncil/*
!.devcouncil/
!.devcouncil/config.yaml

# bench/record.sh holds this while a recording runs and removes it on exit.
bench/results/.record.lock/
